eureka:
  instance:
    hostname: ${APP_URL:localhost}
server:
  port: 8080
spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
      - id: patients
        uri: lb://patient-consumer
        predicates:
        - Path=/api/patients/**